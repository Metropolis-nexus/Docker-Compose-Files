# Notes

ChangeDetection is single user at the moment, so what's in here is just a template to deploy it for different people. 
We use Docker Compose instead of Podman because it is faster to copy & paste.